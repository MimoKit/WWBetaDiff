# WWBetaDiff

鸣潮体验服 V1 / V2 / V3 差异查询插件，数据来自 `nanoka.cc` 的版本化静态 JSON。

## 指令

- `ww diff`：查看当前三个体验服快照的角色、武器变动总览。
- `ww diff 景燃`：查看指定角色或武器的两阶段详细差异。
- `ww diff 1212`：也可以按数据 ID 查询。

插件会缓存成功获取的清单和差异数据，数据源短暂不可用时自动使用最近缓存。
